import org.checkerframework.checker.tainting.qual.Untainted;

package java.util;

interface Iterator<E extends @Untainted Object> {
    @Untainted E next();
}

class Collections {
    static <T extends @Untainted Object> List<T> emptyList();
}
